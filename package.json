{
    "name": "@reliable/sdk",
    "version": "0.0.0",
    "private": true,
    "description": "Reliable browser SDK monorepo (core + framework bindings)",
    "packageManager": "pnpm@9.0.0",
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm -r --parallel dev",
        "typecheck": "pnpm -r typecheck",
        "clean": "pnpm -r clean"
    },
    "devDependencies": {
        "typescript": "^5.5.4",
        "tsup": "^8.3.0"
    }
}
